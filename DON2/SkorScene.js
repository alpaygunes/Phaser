'use strict'

var configSkorScene = {
    key: 'SkorScene' 
};
class SkorScene extends Phaser.Scene {
    constructor() { 
        super(configSkorScene)
    }

    preload() {}

    create() {   }

    update() {} 
}